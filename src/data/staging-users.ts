/**
 * Staging environment credentials for OrangeHRM demo.
 * The staging username differs from the test environment.
 * Used when ENV is set to "staging" (e.g., via `npm run staging`).
 */
export default {
    "username" : "Admin123",
    "password" : "admin123"
}