/**
 * Default test environment credentials for OrangeHRM demo.
 * These are the standard admin credentials for the public demo site.
 * Used by UI login tests and global setup when ENV is set to "test".
 */
export default {
    "username" : "Admin",
    "password" : "admin123"
}